# Frontend env. Copy to .env.local for `npm run dev`, set the same vars in
# Netlify (or whichever host you use) for production builds.

# Base URL of the FastAPI listing service (no trailing slash).
# Local dev:  http://localhost:8000
# Hetzner:    https://api.dev.boliganalyse.ai
# Production: https://api.boliganalyse.ai
VITE_API_URL=http://localhost:8000
